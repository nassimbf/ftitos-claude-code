node_modules/
.DS_Store
*.bak.*
.ftitos-cc-manifest.json

# v4 removals — git history is the archive
.archive/

# built artifacts (58MB compiled driver — built on install, not committed)
skills/browse/dist/
